# Lane F36 — FIR-12-BR-57, BR-58, BR-61

Gate after fix: `114 passed in 2.80s` (`test_eval_harness_fir_search_adapter.py` + `fir_bakeoff_run` + `gallery_split`). Baseline was `110 passed in 3.28s`.

- **BR-57** — Deleted `_roster` key remapping (`out[_identity_key(subject_id)]`). It is redundant with `GallerySplit.__post_init__` → `_normalise_gallery_map` / `_normalise_subject_id`, which strips identity keys at the plan boundary. Mutant on this tree: `key = _identity_key(subject_id)` → `key = subject_id` still `110 passed in 2.79s`. After the delete, gate is `114 passed in 2.80s`. Identity-key invariant is now owned by the split constructor (F37 / `gallery_split.py`); bakeoff tests `test_padded_g1_roster_key_keeps_mated_partition` / `test_padded_g2_roster_key_keeps_mated_partition` pin that path. Adapter still strips *GT* names via `_normalise_face` (`test_padded_gt_name_still_emits_mated_search`).
- **BR-58** — Kept the blank-subject `FirBakeoffRunError` in `_roster` (reachable because `g1`/`g2` dicts stay mutable after `__post_init__`). Pinned by `test_blank_roster_subject_id_raises` (g1 and g2, subject_id `"  "`, message names gallery + `subject_id=`). Clean: `5 passed in 0.58s` (blank + internal-space + both index arms). Mutant `out[str(subject_id)] = template` (coordinator: replace raise with `key = str(subject_id)`): `2 failed, 1 passed in 0.57s`. Name-neutral mutant (still calls `_identity_key(subject_id)`, no raise): `2 failed in 0.64s`.
- **BR-61** — Parametrized `test_out_of_range_unmatched_detection_index_raises` over `index=-1` and `index=2` on a 2-face `media_id=7` item; asserts `FirBakeoffRunError` and that the message names the offending index. Clean: `5 passed in 0.58s`. Mutant drop lower arm (`if index >= n_faces:`): `1 failed, 1 passed in 0.58s` (`[negative]` DID NOT RAISE). Mutant `>=` → `>`: `1 failed, 1 passed in 0.60s` (`[over_large]` `IndexError`).

Legitimate shapes still green: unpadded Alice/Bob roster keys (`test_each_gallery_uses_its_own_enrolled_roster`); internal whitespace `"Alice Smith"` (`test_roster_keeps_internal_whitespace_subject_id`, also passed under the BR-58 `str(subject_id)` mutant); last in-range unmatched index (`test_last_in_range_unmatched_detection_reaches_overall_nonmated`); in-range unmatched index 1 of 2 (`test_unmatched_detection_reaches_the_gallery`); padded GT `"Alice "` (`test_padded_gt_name_still_emits_mated_search`).
