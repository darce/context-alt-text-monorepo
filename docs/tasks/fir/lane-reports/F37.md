# F37 — FIR-12-BR-59, BR-60

TEST-ONLY. `gallery_split.py` untouched. Gate: 113 passed in 3.95s (was 110).

## BR-59
Pinned `_normalise_gallery_map` collision raise in `test_post_normalisation_key_collision_raises_with_offending_raw_value`: `GallerySplitError` plus raw `'Alice '` in the message (EVAL-18 / TEST-15). Mutant `if key in out:` → `if False:` in `/tmp/f37-app-br59/scripts/eval_harness/gallery_split.py`. Clean: `1 passed in 0.04s`. Mutant: `FAILED ... DID NOT RAISE GallerySplitError` / `1 failed in 0.04s`. Message-only mutant dropping `(offending value {subject_id!r})`: `FAILED ... assert "'Alice '" in "g1 subject_id 'Alice' collides after normalisation"` / `1 failed in 0.04s`. Name-neutral rename of `key`/`out`/`subject_id` locals: `1 passed in 0.04s` (test does not denylist symbol names).

## BR-60
Pinned both sorts in `test_gallery_and_probe_order_are_sorted_independent_of_insertion` (EVAL-13): reverse-inserted g1 keys Bob→Alice and probes Zoe→Ann. Mutant `_normalise_gallery_map` `return {subject: out[subject] for subject in sorted(out)}` → `return out` in `/tmp/f37-app-br60map/.../gallery_split.py`. Clean: `1 passed in 0.04s`. Mutant: `FAILED ... assert ['Bob', 'Alice'] == ['Alice', 'Bob']` / `1 failed in 0.04s`. Mutant `_normalise_template_tuple` `return tuple(sorted(out, key=lambda t: (t.subject_id, t.template_id)))` → `return tuple(out)` in `/tmp/f37-app-br60tuple/.../gallery_split.py`. Clean: `1 passed in 0.04s`. Mutant: `FAILED ... assert [('Zoe', 'z1'), ('Ann', 'n1')] == [('Ann', 'n1'), ('Zoe', 'z1')]` / `1 failed in 0.04s`. Map-only mutant leaves collision test green; tuple-only mutant leaves collision test green.

## Legitimate shapes still green
- `test_distinct_stripped_gallery_keys_are_kept`: `"Alice"` + `"Bob "` (distinct after strip) constructs; g1 keys `['Alice', 'Bob']`; two probes `"Ann "` / `"Ann"` kept (not a map collision). Stays green on BR-59 `if False:` and on map-sort deletion (`1 passed in 0.04s` each).
- Existing BR-56 blank / None gallery keys still rejected (gate 113).
- Reverse-ordered distinct identities remain legal; they are sorted, not raised.
