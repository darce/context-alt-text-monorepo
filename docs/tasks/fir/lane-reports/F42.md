# Lane F42 — FIR-12-BR-67

Script lives at repo-root `scripts/regen_eval_report.py` (the listed `apps/prototype-description-service/scripts/regen_eval_report.py` path does not exist). Tests load that repo-root script.

**All five previously-red `test_real_cli_*` cases now pass on their own merits** (exit 3 refused / exit 1 partial / no publish on refused-or-partial / no `partial-corpus` greenwash / identification audit trail on `--allow-refused`). None of those assertions were weakened.

## Gates

| | narrow `test_regen_eval_report_gate.py` | full `scene/tests` + `scripts/eval_harness/tests` |
|---|---|---|
| before | `5 failed, 13 passed in 0.96s` | 5 failed, 1648 passed, 4 skipped, 1 deselected |
| after | `23 passed in 6.84s` | `1658 passed, 4 skipped, 1 deselected, 14 warnings in 83.10s` |

The +10 vs the coordinator's 1653 target is 5 recovered real-CLI cases + 5 new resolver tests.

## FIR-12-BR-67

`resolve_eval_python` reads `ACX_EVAL_PYTHON` when set; a set-but-unusable path raises `EvalPythonError` (`invalid ACX_EVAL_PYTHON: not an executable file: …`, exit 2) and does **not** fall back to `.venv` or `sys.executable`. Unset keeps the original `.venv/bin/python` + `missing service venv python: …` / return 2.

The five `test_real_cli_*` cases now pass `ACX_EVAL_PYTHON=sys.executable`. New coverage: `test_invalid_override_errors_and_does_not_fall_back` (non-executable `neutral-helper.bin` with stub `.venv` present so silent fall-back would go green), `test_unset_override_missing_venv_keeps_original_error`, plus three `resolve_eval_python` unit cases.

TEST-15 `/tmp` mutants (same 5 resolver tests):

- clean copy: `5 passed in 0.25s`
- mutant 1 — delete the override branch entirely: `4 failed, 1 passed in 0.38s` (`test_unset_override_missing_venv_keeps_original_error` still passed)
- mutant 2 — invalid override falls back silently to the default path: `2 failed, 3 passed in 0.33s` (`test_invalid_override_errors_and_does_not_fall_back` exit 0 via stub `.venv`)

Legitimate shapes still green: unset + stub `.venv` (`test_stub_clean_score_publishes_and_exits_0`, unrecognized/allow-refused stub cases); override = `sys.executable` (all five real-CLI cases); override = executable not named `python` (`test_resolve_eval_python_accepts_neutrally_named_executable` uses `worker`); empty `ACX_EVAL_PYTHON` is invalid, not unset, even when `.venv` exists.
