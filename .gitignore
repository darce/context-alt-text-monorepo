# Wordpress - ignore core, configuration, examples, uploads and logs.
# https://github.com/github/gitignore/blob/main/WordPress.gitignore

# Core
#
# Note: if you want to stage/commit WP core files
# you can delete this whole section/until Configuration.
/wp-admin/
/wp-content/index.php
/wp-content/languages
/wp-content/plugins/index.php
/wp-content/themes/index.php
/wp-includes/
/index.php
/license.txt
/readme.html
/wp-*.php
/xmlrpc.php

# Configuration
wp-config.php

# Example themes
/wp-content/themes/twenty*/

# Example plugin
/wp-content/plugins/hello.php

# Uploads
/wp-content/uploads/

# Log files
# *.log

# archives
apps/archived-recognition-service/**
apps/archived-wp-context-alt-text/**

# htaccess
/.htaccess

# All plugins
#
# Note: If you wish to whitelist plugins,
# uncomment the next line
#/wp-content/plugins

# All themes
#
# Note: If you wish to whitelist themes,
# uncomment the next line
#/wp-content/themes

# Reference library (PDFs, extracted notes, marketing — not project planning)
literature/
.DS_Store
# .vscode/settings.json
# .vscode/
.env
.env.*
!.env*.example
!apps/prototype-wp-alt-context/.env.local.example
apps/prototype-wp-alt-context/.env.local
apps/prototype-wp-alt-context/tests/e2e/.auth/
apps/prototype-wp-alt-context/local/playwright/
# visual:localwp snapshot baselines are LocalWP/dev-specific (live media table) — regenerate per machine, not committed.
apps/prototype-wp-alt-context/tests/e2e/visual/*-snapshots/
node_modules/
package-lock.json
!apps/prototype-wp-alt-context/package-lock.json
public/assets/dist/
storybook-static/
vendor/
composer.lock
.phpunit.result.cache

# Vite build output
**/public/assets/dist/
/dist/
/build/

# Python virtual environments (should only exist in apps/recognition-service)
.venv/
venv/
__pycache__/
*.pyc
.hypothesis/

# pip install artifacts
.eggs/
*.egg-info/

# Logs (all service / tool logs)
logs/
apps/prototype-description-service/logs/

# Operational ephemera (local)
CURRENT_TASK.json
CURRENT_TASK.md
DASHBOARD.txt
DASHBOARD.md
.task-state/

# --- workstate-bootstrap-managed surfaces -------------------------------
# Regenerate via `workstate-bootstrap install` from the pinned remote_sha in
# .workstate-bootstrap.json (which IS tracked — it is the install ledger).
# SHARED entries are symlinks into .workstate/remote/; committing them would
# leave dangling symlinks on a fresh clone. GENERATED entries are deterministic
# generator outputs. .mcp.json / .vscode/mcp.json / .codex/config.toml are kept
# tracked here (not gitignored) because they carry the consumer-specific
# context7 server that the workstate generator does not manage.
.workstate/
/scripts/hooks
/.github/hooks
/scripts/workstate
/Makefile.d
/.github/prompts/
/.claude-plugin/
/.agents/

# local logs
/docs/agentic/oci-example/*.log

# local retry logs
/retry-apply.log

# Per-user Claude Code permissions allowlist (machine-specific paths)
.claude/settings.local.json

# Local linked Claude worktrees are ephemeral per-machine artifacts.
.claude/worktrees/

# Local branch/worktree inventory files are ephemeral operator notes.
local/*-worktrees.txt

# Ephemeral Grok workstate bootstrap overlay (machine-local).
/.grok/plugins/workstate-system
