# Lane F40 — FIR-12-BR-65, BR-66

Did not touch `gallery_split.py`, `_normalise_gallery_map`, or `_as_template`. Tests-only pin of two already-correct call sites.

Narrow gate: baseline `121 passed in 3.41s` → after `126 passed in 2.87s`.
Full gate: baseline `5 failed, 1648 passed, 4 skipped, 1 deselected` in 73.91s → after `5 failed, 1653 passed, 4 skipped, 1 deselected` in 73.21s (same 5 `test_real_cli_*` env failures).

- **BR-65** — Pinned `_normalise_gallery_map` and `_as_template` key-vs-`subject_id` raises. `test_gallery_map_key_subject_mismatch_raises[g1|g2]` constructs `GallerySplit` with key `"Alice"` / template `"Carol"` (g1) and key `"Bob"` / template `"Carol"` (g2); asserts `GallerySplitError` and that `repr` of both identities is in the message. `test_builder_roster_key_subject_mismatch_raises` feeds Carol templates under roster key `"alice"` through `build_disjoint_galleries`. Clean: `5 passed in 0.17s`. Map mutant `/tmp/f40-br65-map` (deleted `if template_key != key: raise`): `2 failed, 3 passed in 0.18s` (`[g1]` and `[g2]` DID NOT RAISE; builder/padding/withheld stayed green). `_as_template` mutant `/tmp/f40-br65-as-template` (deleted roster-key raise): `1 failed, 4 passed in 0.18s` (builder DID NOT RAISE; constructor mismatch still red-able via the other arm). Tests assert exception + identity strings, not symbol names.

- **BR-66** — Pinned the constructor call site. `test_withheld_probe_templates_are_sorted_and_stripped_independent_of_insertion` passes reverse-ordered padded `("Zoe ", "Ann ")` withheld templates *directly* into `GallerySplit`, not through `build_disjoint_galleries`. Asserts `[("Ann", "n1"), ("Zoe", "z1")]`. Clean: same `5 passed in 0.17s`. Mutant `/tmp/f40-br66-withheld` (`tuple(self.withheld_probe_templates)` instead of `_normalise_template_tuple(...)`): `1 failed, 4 passed in 0.17s` — got `[('Zoe ', 'z1'), ('Ann ', 'n1')]` (insertion order + trailing spaces).

## Legitimate shapes still green

- Key `" Alice "` + `subject_id` `"Alice"`; key `"Alice"` + `subject_id` `" Alice "`; g2 key `" Bob "` + `subject_id` `"Bob"` (`test_gallery_key_and_subject_id_padding_is_same_identity`).
- Builder roster `" alice "` with templates `"alice"` / `" alice "` — strips to the same identity, still a legal split.
- Exact-match Alice/Bob maps; internal whitespace `"Alice Smith"` (`test_internal_whitespace_subject_id_is_kept`); distinct stripped keys Alice vs `"Bob "` (`test_distinct_stripped_gallery_keys_are_kept`).
- BR-60 probe-template reverse-insert sort (`test_gallery_and_probe_order_are_sorted_independent_of_insertion`); empty withheld default `()` on every prior constructor test; frozen-frame seed 0 `g1=53 g2=56`.
