# APP-1 S0 E16-7 disposition matrix

| Finding ID | One-line claim | Current-state verdict | Concrete file:symbol evidence | What S0 must still do |
| --- | --- | --- | --- | --- |
| `E167FI-H-01` | A portal key read or mutation must be structurally bound to the server-resolved tenant. | still-open | `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository`; `apps/prototype-description-service/recognition/interface_adapters/http/deps/tenant_common.py::get_tenant_id` | Assign the tenant-bound repository and portal service owner; prohibit caller-supplied tenant identifiers. |
| `E167FI-H-02` | A restore must not make a revoked credential valid again. | still-open | `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::RAW_SQL_TABLES`; `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository.revoke` | Choose one recovery authority, WAL/PITR or an append-only revocation journal, and name the drill owner and residual tail. |
| `E167FI-H-03` | Clerk identity must bind once to a stable issuer and subject rather than a mutable email address. | still-open | `apps/prototype-description-service/api/main.py::create_app`; `apps/prototype-description-service/recognition/infrastructure/repositories/tenant_repository.py::SqlAlchemyTenantRepository` has no portal identity-link surface | Decide the first-claim and subsequent-session state machine, including concurrent claims and email changes. |
| `E167FI-H-04` | Cookie-authenticated portal mutations need a precisely defined CSRF token and same-origin rule. | needs-decision | `apps/prototype-description-service/recognition/interface_adapters/http/deps/admin_auth.py::require_same_origin` covers admin requests only; no portal CSRF callable is mounted by `apps/prototype-description-service/api/main.py::create_app` | **Open question OQ-E167FI-H-04:** choose the token derivation, accepted headers, cookie/bearer split, and exact 403 contract. |
| `E167FI-H-05` | Server-rendered HTML mutations need an explicit transport for CSRF and idempotency fields. | needs-decision | `apps/prototype-description-service/recognition/interface_adapters/http/admin_console.py::render_console` renders existing admin forms; no `portal_console` surface exists | **Open question OQ-E167FI-H-05:** choose hidden-form versus header transport and define how the browser page and bearer API share mutation semantics. |
| `E167FI-H-06` | Rotation ownership belongs in a tenant-bound repository/service, not an unscoped key-ID helper. | still-open | `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository.revoke` accepts a key ID; no `TenantBoundApiKeyRepository` or rotation repository exists | Name the shared rotation/revoke service and repository owner and require tenant predicates before reservation or mutation. |
| `E167FI-H-07` | Rotation, predecessor cutoff, audit evidence, and replay metadata must commit atomically. | still-open | `apps/prototype-description-service/recognition/application/services/api_key_admin_service.py::mint_api_key` leaves commit ownership to its caller; `apps/prototype-description-service/recognition/interface_adapters/http/routers/admin.py::revoke_key_atomic` only covers direct revoke | Define the portal transaction boundary, idempotency conflict replay, and rollback behavior before implementation. |
| `E167FI-H-08` | Portal authentication and configuration must fail closed without weakening recognition or the Tailscale-only admin boundary. | still-open | `apps/prototype-description-service/api/main.py::create_app` mounts recognition, demo, and conditional admin routers but no portal router or Clerk boot validator | Decide mount gating, required settings, verifier outage response, and `/admin*` behavior on the portal host. |
| `E167FI-M-01` | A tenant must see complete key history, including revoked metadata, without cross-tenant rows. | still-open | `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository.list_for_tenant`; no portal read adapter is present | Specify the bound read API and its isolation proof. |
| `E167FI-M-02` | Equal rotation idempotency keys must replay one replacement and reject a different request fingerprint. | still-open | No `portal_rotations` table or rotation handler is registered by `apps/prototype-description-service/api/main.py::create_app`; `apps/prototype-description-service/db/models/identity.py::CurationReplayRecord` is unrelated prior art | Decide the reservation key, fingerprint normalization, replay envelope, and conflict status. |
| `E167FI-M-03` | Routine rotation must cap predecessor overlap without extending an earlier expiry. | still-open | `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository.revoke` has no cutoff operation; no rotation service exists | Set the seven-day maximum, original-expiry rule, and active/expired/revoked state mapping. |
| `E167FI-M-04` | A newly minted raw key must be displayed once and excluded from logs, audit rows, and URLs. | already-satisfied | `apps/prototype-description-service/recognition/application/services/api_key_admin_service.py::mint_api_key` generates the raw token and stores only its digest; `apps/prototype-description-service/recognition/interface_adapters/http/admin_console.py::_minted_block` renders the one-time value | Reuse this secret-handling contract in portal rotation and prove it for the new adapter; no new key-storage rule is needed. |
| `E167FI-M-05` | Direct revoke must be a conditional write so concurrent requests have one winner. | already-satisfied | `apps/prototype-description-service/recognition/interface_adapters/http/routers/admin.py::revoke_key_atomic` owns the existing atomic revoke outcome and audit transaction | Extract and reuse the operation for portal mapping; preserve the admin response contract while mapping portal losers to 409. |
| `E167FI-M-06` | A failed audit insert must roll back the credential mutation. | already-satisfied | `apps/prototype-description-service/recognition/interface_adapters/http/routers/admin.py::revoke_key_atomic` records audit before commit in the same session; `apps/prototype-description-service/recognition/application/services/audit_service.py::AuditService.record_event` is called before that commit | Verify the extracted shared service preserves rollback for rotate and revoke; do not duplicate the transaction in the portal router. |
| `E167FI-M-07` | Recognition-data reset privileges must be separated from customer credential and portal recovery authority. | still-open | `apps/prototype-description-service/api/main.py::create_app` has no portal recovery surface; `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::heal` owns schema repair without a portal privilege model | **Open question OQ-E167FI-M-07:** name database roles, reset operators, journal/WAL readers, and the recovery approval boundary. |
| `E167FI-M-08` | Existing key validation must distinguish active, expired, and revoked credentials consistently. | already-satisfied | `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository.get_by_hash`; `apps/prototype-description-service/recognition/infrastructure/repositories/api_key_repository.py::SqlAlchemyApiKeyRepository.classify_by_hash`; `apps/prototype-description-service/recognition/interface_adapters/http/deps/auth.py::require_auth` maps lookup state to authentication outcomes | Reuse the classifier for the old-key overlap and portal error mapping; do not introduce a second key-state vocabulary. |
| `E167FI-M-09` | Rotation restore must replay enough durable evidence to keep a revoked predecessor invalid. | still-open | `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::RAW_SQL_TABLES` has no `portal_revocation_journal`; `apps/prototype-description-service/scripts` has no portal-revocation shipper | **Open question OQ-E167FI-M-09:** select WAL/PITR or the journal proposal, then record the measured recovery tail and probe. |
| `E167FI-M-10` | A client-supplied tenant header or query value must not select a portal tenant. | still-open | `apps/prototype-description-service/recognition/interface_adapters/http/deps/tenant_common.py::get_tenant_id` resolves `X-Tenant-ID` and query input; `apps/prototype-description-service/recognition/interface_adapters/http/deps/auth.py::require_auth` is recognition auth, not Clerk portal binding | Define a separate portal dependency that ignores request tenant selectors and uses the verified identity link. |
| `E167FI-M-11` | Unmatched, unverified, or incomplete portal identity claims need distinct non-leaking outcomes and durable evidence. | still-open | `apps/prototype-description-service/recognition/observability/auth_audit.py::emit_auth_event` is recognition audit instrumentation; no `portal_auth_events` table is in `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::RAW_SQL_TABLES` | Define 401 versus 403 outcomes, hashed-email policy, and one pre-tenant event per rejection. |
| `E167FI-M-12` | Portal auth-event writes must not reuse a request transaction or turn telemetry failure into tenant access. | still-open | `apps/prototype-description-service/recognition/interface_adapters/http/deps/session.py::get_session`; `apps/prototype-description-service/recognition/observability/auth_audit.py::emit_auth_event` has no portal-specific short-lived writer | Define the isolated writer, error log fields, and response behavior when the event insert fails. |
| `E167FI-M-13` | A request carrying both an ambient session cookie and a bearer credential must be rejected before either credential is trusted. | still-open | `apps/prototype-description-service/recognition/interface_adapters/http/deps/auth.py::require_auth` handles API keys only; `apps/prototype-description-service/api/main.py::create_app` has no Clerk portal dependency | Decide the dual-credential precedence and `invalid_session` contract. |
| `E167FI-M-14` | A Clerk verifier outage must fail portal auth distinctly while valid API-key recognition remains available. | still-open | `apps/prototype-description-service/api/main.py::create_app` has separate recognition mounting but no Clerk verifier; `apps/prototype-description-service/recognition/interface_adapters/http/deps/auth.py::require_auth` does not call Clerk | Define bounded JWKS timeout/cache and prove the portal and recognition failure domains are separate. |
| `E167FI-M-15` | The new portal host must not expose the operator `/admin` surface. | still-open | `apps/prototype-description-service/api/main.py::create_app` conditionally includes `apps/prototype-description-service/recognition/interface_adapters/http/routers/admin.py::router`; no host-specific portal boundary exists | Assign the Caddy/host rule and an app-level `/admin*` 404 test. |
| `E167FI-M-16` | The portal page should use escaped server-rendered HTML without introducing a template runtime or secret replay. | already-satisfied | `apps/prototype-description-service/recognition/interface_adapters/http/admin_console.py::render_console`; `apps/prototype-description-service/recognition/interface_adapters/http/admin_console.py::_esc` | Reuse the escaping and one-time rendering pattern in `portal_console`; add portal-specific tests rather than a new templating system. |
| `E167FI-M-17` | Identity, rotation, auth-event, and journal tables need explicit schema checks, RLS classification, and migration registration. | still-open | `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::TENANT_TABLES`; `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::EXPECTED_SCHEMA_TABLES` has no portal tables | Set the table ownership/RLS matrix and update the single schema source of truth. |
| `E167FI-M-18` | Tenant-scoped rotation records need forced RLS and repository predicates independent of the exempt key tables. | still-open | `apps/prototype-description-service/db/tenant_context.py::set_tenant_context`; `apps/prototype-description-service/db/models/scene.py::DescribeDemandLease` shows tenant-scoped ORM prior art, but no portal rotation model exists | Decide which portal tables are RLS-exempt versus tenant-scoped and require repository-level isolation tests. |
| `E167FI-M-19` | A concurrent equal-idempotency retry must reapply tenant context before replaying committed metadata. | still-open | `apps/prototype-description-service/db/tenant_context.py::set_tenant_context`; no `portal_rotations` repository or replay transaction exists | Specify rollback/rebind behavior and the transaction that owns replay. |
| `E167FI-M-20` | Losing the first rotation response must not strand the tenant without a recoverable next action. | still-open | `apps/prototype-description-service/recognition/application/services/api_key_admin_service.py::mint_api_key` has no rotation reservation or recovery metadata; `apps/prototype-description-service/api/main.py::create_app` has no portal routes | Define lost-response guidance, replay secrecy, and new-key rotation behavior. |
| `E167FI-M-21` | The existing admin already-revoked response and audit cardinality must survive service extraction. | already-satisfied | `apps/prototype-description-service/recognition/interface_adapters/http/routers/admin.py::revoke_key_atomic` returns an already-revoked outcome without a second audit commit | Treat this as a regression constraint for the shared service; S0 need not redesign admin semantics. |
| `E167FI-M-22` | Portal invalid-token, ineligible-contact, and CSRF failures must keep 401/403 meanings and avoid tenant enumeration. | still-open | `apps/prototype-description-service/recognition/interface_adapters/http/deps/auth.py::require_auth` covers API-key errors only; no portal error adapter is mounted by `apps/prototype-description-service/api/main.py::create_app` | Define the portal error map and event/audit visibility without leaking tenant existence. |
| `E167FI-M-23` | Pre-tenant identity evidence must hash email and never persist raw token or email material. | still-open | `apps/prototype-description-service/recognition/observability/auth_audit.py::emit_auth_event` is not a portal schema writer; `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::RAW_SQL_TABLES` lacks `portal_auth_events` | Decide the exact event columns, hash normalization, retention, and logging redaction. |
| `E167FI-M-24` | The portal eval gate must fail on missing or skipped evidence instead of silently passing. | still-open | `apps/prototype-description-service/api/main.py::create_app` and the current test tree contain no E16-7 portal eval checker; `docs/scopes/e16-7-tenant-selfserve-key-panel-evals.json` is only a provisional manifest | Assign the checker/test owner and require zero skips plus nonzero on missing JUnit. |
| `E167FI-M-25` | Production provisioning and recovery instructions must make the safe operator path explicit. | still-open | `apps/prototype-description-service/Makefile::provision-customer` is the existing local provisioning target; no `provision-customer-oci` target or portal recovery implementation exists | Decide the OCI-only production mint path, refusal behavior, and runbook/restore evidence owner. |
| `E167FI-L-01` | The server-rendered portal must provide a bounded, accessible key-management surface without shipping Clerk JavaScript. | still-open | `apps/prototype-description-service/recognition/interface_adapters/http/admin_console.py::render_console` is admin-only; `apps/prototype-description-service/api/main.py::create_app` mounts no portal router | Define the minimal portal page, hosted sign-in redirect, and accessibility/security acceptance tests. |
| `E167FI-L-02` | Restore and operational ownership must be evidenced before customer exposure, not inferred from a backup file. | still-open | `apps/prototype-description-service/db/migrations/versions/001_identity_schema.py::heal`; `apps/prototype-description-service/recognition/interface_adapters/http/routers/admin.py::revoke_key_atomic` provide local state changes but no off-host restore drill | Name the operator, artifact retention policy, measured RTO/RPO, and revoked-key probe. |
