# Florence-2 benchmark/eval seed images

Drop a real .jpg/.png here to use as a checked-in benchmark fixture for
`scripts/benchmark_local_vlm.py`. No third-party photo is vendored; the eval
accepts arbitrary image paths as args. See the decision memo in docs/tasks/19.0/.
