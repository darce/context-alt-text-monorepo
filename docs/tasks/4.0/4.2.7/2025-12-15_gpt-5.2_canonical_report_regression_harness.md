# 2025-12-15 — Canonical Recognition Report + Regression Harness

Author: OpenAI GPT-5.2

## Why this exists

Accuracy is declining and it’s currently hard to tell whether a change improved clustering or simply shifted manual curation work around. The goal is to create a **repeatable testing harness** that can detect progress/regressions in the recognition/clustering pipeline without requiring re-labeling every time.

This document proposes a “canonical report” that is generated **after** the user finishes labeling clusters and doing merges/splits, and a harness that compares future runs against that canonical snapshot.

Related docs in this folder:
- `docs/tasks/4.0/4.2.7/representative_recomputation_plan.md`
- `docs/tasks/4.0/4.2.7/clustering_fixes_implementation_plan.md`
- `docs/tasks/4.0/4.2.7/clustering_pipeline_diagnostic.md`
- `docs/tasks/4.0/4.2.7/hdbscan_inconsistency_analysis.md`

---

## Definitions (so metrics are unambiguous)

- **Identity**: one detected face record (`media_identities`), i.e. a face bounding box on a media item.
- **Identity locator**: stable identifier for a face across DB resets: (`media_id`, `bbox_x`, `bbox_y`, `bbox_width`, `bbox_height`).
- **Cluster**: `identity_clusters` grouping of identities.
- **Labeled cluster**: `identity_clusters.user_confirmed=true` (and usually `label` non-null).
- **Canonical label**: the human “ground truth” class, derived from labeled clusters after curation is complete.
- **Original assignment**: the cluster an identity was assigned to by the pipeline before any manual curation.
- **Curation actions**:
  - **Merge**: entire cluster A merged into cluster B.
  - **Split**: identities moved out of a cluster into other cluster(s) (manual remove/assign).
  - **Wrong person (contamination)**: identities that ended up removed from a labeled cluster (false merge symptom).
  - **Fragmentation**: multiple original clusters that should have been one canonical label (false split symptom).

---

## Proposed output: Canonical report (what it must contain)

The report needs to do two jobs:
1. Act as **ground truth** for a fixed dataset (so future runs can be evaluated automatically).
2. Act as an **error attribution dataset** (so we can tell which pipeline stage/method produced mistakes that the user had to fix).

### Canonical report schema (v1)

Store as JSON (primary), and optionally emit CSV views for quick analysis.

Top-level:
```json
{
  "schema_version": 1,
  "generated_at": "2025-12-15T...",
  "tenant_id": "...",
  "dataset": {
    "name": "optional human name",
    "roster_id": "optional",
    "media_ids": ["41", "42", "..."],
    "notes": "how this dataset was selected"
  },
  "baseline_run": {
    "run_id": "uuid",
    "git_sha": "optional",
    "settings_snapshot": {
      "similarity_threshold": 0.85,
      "min_representatives_for_maturity": 0,
      "max_representatives_per_cluster": 10,
      "hdbscan_max_batch_size": 500
    },
    "embedding_model": {
      "name": "buffalo_l",
      "dimension": 512
    }
  },
  "canonical_clusters": [ /* labeled clusters only */ ],
  "cluster_outcomes": [ /* original cluster -> final outcome */ ],
  "metrics": { /* derived */ }
}
```

Per canonical (labeled) cluster:
```json
{
  "canonical_label": "Ryann Wiseman",
  "final_cluster_id": "uuid (informational; not stable across DB resets)",
  "final_representatives": [
    {
      "identity_locator": {
        "media_id": 702,
        "bbox_x": 123,
        "bbox_y": 45,
        "bbox_width": 80,
        "bbox_height": 80
      },
      "quality_score": 0.92,
      "diversity_score": 0.11,
      "thumbnail_url": "optional",
      "media_url": "optional"
    }
  ],
  "member_identities": [
    {
      "identity_locator": {
        "media_id": 702,
        "bbox_x": 123,
        "bbox_y": 45,
        "bbox_width": 80,
        "bbox_height": 80
      },
      "original_assignment": {
        "cluster_id": "uuid",
        "decision": "accepted|suggested|rejected|new_cluster",
        "method": "representative|centroid|graph|anchor_bypass|manual",
        "stage": "RepresentativeDiscovery|CentroidDiscovery|GraphDiscovery|Curation",
        "similarity": 0.9131,
        "confidence": 0.9131,
        "threshold": 0.85,
        "gate_checks": { "passed": ["..."], "failed": ["..."] },
        "method_details": {
          "best_representative_identity_locator": "present when method=representative",
          "graph_algorithm": "hdbscan|chinese_whispers",
          "graph_params": { "epsilon": 0.55, "min_cluster_size": 3 }
        }
      },
      "curation": {
        "final_label": "Ryann Wiseman",
        "actions": ["merged_in", "moved_out", "manual_assign", "manual_remove"],
        "notes": "optional"
      }
    }
  ],
  "summary": {
    "identity_count": 195,
    "fragmentation_original_cluster_count": 37,
    "contamination_moved_out_count": 4,
    "assignment_method_breakdown": { "representative": 120, "graph": 75 }
  }
}
```

Per original cluster outcome (supports merge/split tracking):
```json
{
  "original_cluster_id": "uuid",
  "original_label": "uuid label or null",
  "original_size": 12,
  "outcome": "kept|merged|split|discarded",
  "final_destinations": [
    { "canonical_label": "Ryann Wiseman", "moved_count": 10 },
    { "canonical_label": "Other Person", "moved_count": 2 }
  ],
  "error_type": "fragmentation|contamination|mixed|clean",
  "method_breakdown": { "graph": 12 },
  "graph_params": { "algorithm": "hdbscan", "epsilon": 0.55, "min_cluster_size": 3 },
  "representative_stats": { "rep_count": 1, "max_reps": 10, "selection": "fps" }
}
```

### Important detail: stable identity matching

If you reset the DB and re-run face detection, `media_identities.id` will change. For regression testing, the harness must match identities across runs using a stable locator:
- (`media_id`, `bbox_x`, `bbox_y`, `bbox_width`, `bbox_height`) from `media_identities`

This assumes face detection is deterministic enough for the dataset. If bbox drift becomes an issue, the next step is to also store a **crop hash** (hash of the face crop bytes) to match robustly.

---

## How to generate the canonical report (implementation plan)

### Phase 0 vs Phase 1 (recommendation)

If the goal is a long-lived regression harness, **Phase 0 log parsing is mostly throwaway** because the current logs are missing key join keys (notably `cluster_id` for `new_cluster`) and omit attribution fields (method + similarity) for accepted/rejected decisions.

To avoid wasting effort:
- Implement the canonical report builder + scoring + graphs as reusable code.
- Go straight to **Phase 1 “runs + events”** for the source-of-truth data.
- Treat any Phase 0 parsing as an optional “log importer” adapter (useful only for backfilling past runs).

### Phase 0 (fast, log-based stopgap)

Goal: generate a usable report from existing `recognition.log.*` files **without waiting** for new DB tables.

1. Identify a “baseline run window”:
   - start: `[clustering] batch_start job_id=...`
   - end: “curation complete” marker (manual) or time window with last `[curation]` event
2. Parse:
   - original assignments: `[clustering] ACCEPTED|SUGGESTED|REJECTED` and `Gate decision ...`
   - new cluster creation events: `[clustering] new_cluster ...` (currently missing `cluster_id`, see “missing metrics” below)
   - curation: `[curation] MERGED|ASSIGNED|REMOVED|RENAMED`
3. Query DB once at the end for:
   - labeled clusters (`identity_clusters.user_confirmed=true`) and their members (`identity_members` -> `media_identities`)
4. Emit report by combining:
   - DB final state (canonical labels + membership)
   - log-derived original assignment + curation timeline (attribution)

This is good enough to start measuring regressions, but it’s brittle (log rotation, missing fields).

### Phase 1 (correct, DB-backed “runs + events”)

Goal: stop depending on logs by persisting a minimal run/event model.

1. Add DB tables (or a single JSONB event table):
   - `recognition_runs`: `id`, `tenant_id`, `created_at`, `git_sha`, `settings_snapshot`, `dataset_selector`
   - `recognition_events`: `id`, `run_id`, `ts`, `event_type`, `payload` (JSONB)
2. Emit events from the pipeline (at least):
   - `assignment_decision` (ALWAYS includes `identity_id` + `identity_locator`, decision, method, similarity, confidence, threshold, gate pass/fail, and best_rep locator when applicable)
   - `cluster_created` (cluster_id + identity_locators of initial members, plus creation_method)
   - `cluster_merged` (source_cluster_id, target_cluster_id, moved_count; persist moved `identity_id`s and/or `identity_locator`s when feasible)
   - `identity_moved` (`identity_id` + `identity_locator`, from_cluster_id, to_cluster_id, reason=user_action)
   - `cluster_renamed` (cluster_id, old_label, new_label)
   - `graph_run` (algorithm selection + parameters + outputs: clusters/outliers/edges, anchors injected)
   - `representatives_snapshot` (cluster_id, rep_count, and representative `identity_id`s + `identity_locator`s + scores)
   - `representatives_recomputed` (cluster_id, rep_count_before/after, selection_method, selected representative identity_locators)
3. Implement `generate_canonical_report` as a CLI script:
   - location: `apps/prototype-description-service/scripts/`
   - input: `--tenant-id`, `--run-id`, optional `--roster-id` or `--media-id-file`
   - output: `data/canonical_reports/<tenant>/<run_id>.json`

This makes the report reproducible and makes per-method attribution straightforward.

---

## Representative images (can we see them?)

Yes. The DB already tracks representative embeddings in `identity_cluster_representatives`:
- Each row includes `cluster_id`, `identity_id`, and representative scores (`quality_score`, `diversity_score`).
- Join `identity_cluster_representatives.identity_id -> media_identities` to recover the representative’s:
  - stable `identity_locator` (`media_id` + bbox coords)
  - `thumbnail_url` (if populated)
  - `media_url`

The canonical report should record (at least):
- the final representatives for each labeled cluster (`final_representatives` in the schema above)
- optionally, the “best representative used” for each representative-based assignment (`best_representative_identity_locator`)

---

## Regression harness (how future runs get scored)

### Inputs

- `canonical_report.json` (ground truth)
- a new run’s assignments for the same dataset (from `recognition_events` or a DB export)

### Core scoring approach (label-permutation safe)

For clustering, avoid “accuracy by label name” because predicted clusters are unlabeled. Use permutation-invariant metrics:
- **Pairwise precision**: among identity pairs predicted in the same cluster, how often do they share the same canonical label? (penalizes false merges)
- **Pairwise recall**: among identity pairs with the same canonical label, how often are they predicted in the same cluster? (penalizes fragmentation)
- **Pairwise F1**
- Optional: ARI / VI / B-cubed (nice-to-have)

### Curation cost metrics (closest to UX pain)

Derived from canonical vs predicted membership:
- **Fragmentation per canonical label**: number of predicted clusters that contain identities of this label
- **Contamination per canonical label**: number of predicted clusters that mix this label with others (or % mixed identities)
- **Estimated merges needed**: predicted_cluster_count_per_label - 1
- **Estimated removals needed**: count of identities in a predicted cluster whose canonical label ≠ cluster’s majority canonical label

### Output

- `metrics.json` (machine-readable)
- `summary.md` (human-readable)
- optional plots (see below)

---

## What we can already measure from `recognition.log.13`

Even without new instrumentation, `apps/prototype-description-service/logs/recognition.log.13` already enables useful metrics:

### Batch-level

From `[clustering] batch_complete ...`:
- accepted / suggested / rejected counts
- new_clusters count
- elapsed time (batch_start -> batch_complete)

Example (same log file):
- `accepted=97 suggested=72 rejected=3 new_clusters=173` (job `0693f1fb-...`)
- `accepted=191 suggested=0 rejected=12 new_clusters=210` (job `0693f627-...`)

### Gate behavior

From `Gate decision ... (checks passed: [...], failed: [...])`:
- decision distribution (accept/suggest/reject)
- per-check failure rate

In `recognition.log.13`, gate decisions are dominated by maturity failures (72 failures recorded), which is consistent with `docs/tasks/4.0/4.2.7/clustering_pipeline_diagnostic.md`.

### RepresentativeDiscovery “near-miss” analysis

From `[RepresentativeDiscovery] NO MATCH ... best_sim=... (threshold=...)`:
- best_sim distribution for non-matches (how close we were to threshold)
- helps answer “are we missing by 0.02 or by 0.30?”

This can be tracked over time to see if representative coverage improves after fixes (e.g., recomputation after merge).

### Curation workload (manual effort proxy)

From `[curation] MERGED|ASSIGNED|RENAMED|REMOVED`:
- number of manual actions per run
- moved_count distribution for merges (which identities/people are most painful)

---

## Metrics that are missing today (and would make this dramatically better)

These are the highest-leverage additions to logs/events to support the canonical report and graphs:

1. **Assignment method on every ACCEPTED/SUGGESTED/REJECTED**
   - Persist: `assignment_decision.method=representative|centroid|graph|anchor_bypass|manual`
   - Log (optional): add `method=... stage=...` to each decision line
2. **Similarity/confidence on ACCEPTED/SUGGESTED/REJECTED**
   - Persist: `assignment_decision.similarity`, `assignment_decision.threshold`, `assignment_decision.confidence`
   - Clarify meaning: `similarity` = cosine similarity; `confidence` = score used for UX/ranking (can equal similarity initially)
3. **`cluster_id` on `new_cluster` log lines**
   - Persist: `cluster_created.cluster_id` and initial member identity_locators
   - Log: include `cluster_id=...` on `[clustering] new_cluster ...`
4. **Graph algorithm parameters**
   - Persist: `graph_run.algorithm`, `graph_run.params`, `graph_run.outputs` (clusters/outliers/edges), `graph_run.anchor_count`
   - Log: one line per graph invocation with the above fields
5. **Representative stats**
   - Persist: `representatives_snapshot` and `representatives_recomputed` (rep_count before/after, selection method, selected identity_locators, scores)
6. **Curation events should include identity ids when possible**
   - Persist `identity_id` (run-local) + stable `identity_locator` for `ASSIGNED` / `REMOVED` / `MERGED` membership changes so canonical reports survive DB resets
   - If payload size is a concern: store full lists in DB events, log only counts + samples

If only one change is made: add a persistent `recognition_events` table and treat logs as best-effort.

---

## How to graph clusters and pipeline efficiency

These visualizations make regressions obvious:

1. **Sankey / alluvial diagram (best ROI)**
   - left: original predicted clusters
   - right: final canonical labels
   - edge width: # identities moved
   - immediately shows fragmentation and contamination

2. **Confusion matrix heatmap**
   - rows: canonical labels
   - cols: predicted clusters (after Hungarian/majority mapping)
   - highlights which identities are swapped/mixed

3. **Cluster size distribution over time**
   - histogram of cluster sizes (especially singleton ratio)
   - track “singleton explosion” regressions

4. **Embedding projection (UMAP/t-SNE)**
   - points = identities
   - color by canonical label; outline by predicted cluster
   - reveals overlap regions and “bridge” faces that cause merges

5. **Curation cost dashboard**
   - merges/splits/removes per canonical label
   - time-to-curation (if timestamps persisted)

Implementation-wise, (1) and (2) can be generated entirely from the canonical report + a run export; (4) requires access to embeddings.

---

## Practical next steps (recommended order)

1. Add minimal **run_id + event persistence** (Phase 1) so canonical reports don’t depend on log rotation or missing fields.
2. Implement canonical report generation + harness scoring (pairwise precision/recall + curation-cost metrics):
   - `generate_canonical_report ...`
   - `evaluate_run --canonical ... --run-id ...`
3. Add 1–2 “golden graphs”: Sankey + size distribution.
4. Optional: implement a Phase 0 log importer only if you want to backfill historical runs.

If you want, I can also draft the concrete CLI interfaces and the exact DB event payload fields to keep the implementation tight.
