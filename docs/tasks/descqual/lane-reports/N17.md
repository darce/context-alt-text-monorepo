# N17 — DESCQUAL-2-BR-53

Gate: `scene/tests/test_eval_harness_pilot_draw.py`. Clean: **67 passed in 3.68s**.

## DESCQUAL-2-BR-53

What changed: `_required_params` now keeps a groups-like parameter even when it has a default, plus `VAR_POSITIONAL` / `VAR_KEYWORD`. `_iter_bindings` still fills named params as before, then also splats the probe groups into `*args` and tries each name in `_GROUPS_PARAM_NAMES` as a `**kwargs` key. Empty `[((), {})]` remains only when there is nothing to bind. No name denylist change.

Tests: `test_estimator_with_optional_or_variadic_groups_is_rejected` (`_beta_share` defaulted `groups`, `_payload_fold(*groups)`, `_slot_pack(**kwargs)` looking up `kwargs["groups"]`) and `test_ordinary_optional_or_variadic_helper_is_not_a_reliability_estimator` (`_plain_mean`, `_sum_fold(*values)`, `_label_join(**kwargs)`). Neutral names only — no `icc` / `deff` / `reliability` / `_DESIGN_STAT_KEYS` tokens.

Legitimate shapes still GREEN (inject + `_assert_no_reliability_estimator` where noted):

- `_plain_mean(xs=(1.0, 2.0, 3.0, 4.0))` defaulted non-groups arg
- `_sum_fold(*values)` mean, not ICC
- `_label_join(**kwargs)` key join, not ICC
- existing `_ORDINARY_HELPER_SOURCES`: annotated/unannotated kw-only and positional pairs, `_join_labels`
- coordinator five: generator `_walk_items`, plain mean `_plain_avg`, `**kwargs` `_kw_echo`, unannotated pair `_pair_join`, defaulted `_defaulted_tag`
- existing `pilot_draw` helpers via the clean suite (`_require_int`, `_gold_count`, `_pick_one`, `_parse_stratum`, `_is_hard_entry`, `_load_selection_payload`, …)

TEST-15 mutant (throwaway copy under `/home/ubuntu/br53-proof/test_br53_reverted_bindings.py`): restore pre-BR-53 `_required_params` / `_iter_bindings` (drop defaulted groups, drop `*args` splat, drop `**kwargs` groups names). Cases 1–3 go GREEN on the guard (tests RED); 4–6 and the rest stay put.

- Clean: **67 passed in 3.68s**
- Reverted bindings: **3 failed, 64 passed in 5.66s** — `test_estimator_with_optional_or_variadic_groups_is_rejected[defaulted_groups|varargs_groups|kwargs_only_groups]` (`assert '_beta_share' in []` / `'_payload_fold' in []` / `'_slot_pack' in []`)
