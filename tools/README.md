# Developer Tools

Place local automation scripts, Make targets, or helper binaries here. Example subdirectories:

- `scripts/` — shell or Python utilities (e.g., `sync-schemas.sh`, `deploy-hf.sh`).
- `ci/` — reusable GitHub Actions workflow fragments.

Keep tooling separated from application code so builds remain reproducible.
