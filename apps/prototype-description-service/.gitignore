scripts/eval_harness/out/
# bake-off scratch when run from the app root (canonical copies live in
# docs/tasks/altq/bakeoff-results/)
/out/
# Large ONNX weights are operator-fetched, never commit-eligible (FIR-3 S1 BR-10).
*.onnx
*.onnx.partial
