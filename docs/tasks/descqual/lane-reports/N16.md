# N16 — DESCQUAL-2-BR-51 / BR-52

Gate: `scene/tests/test_eval_harness_pilot_draw.py`. Clean: **61 passed in 2.51s**.

Discriminator is behaviour (does the callable return a one-way ICC?), not the symbol name and not whether the author wrote type hints.

## Choice (BR-51)

Both arms, not "raises on every probe is a violation":

1. Probe fixtures now include k=5, 8, 16, 32, 64 (plus the original k=3,4). k=5 is the coordinator mutant; larger k covers a plausible min-n guard without pretending every raise is an estimator.
2. A raise on one fixture no longer aborts the rest. Any successful call that returns the one-way ICC is a violation.

Rejected "all-raise = fail": `_gold_count`, `_load_selection_payload`, `_is_hard_entry`, `_parse_stratum` all raise on the groups payload. That would fail the existing module.

## BR-51

What changed: skip-on-raise + k∈{3,4}-only probes replaced with skip-that-fixture-and-keep-going plus k≥5 probes. Unfilled extra args are dummy-filled (groups payload, None, 1.0, True, (), ones-vectors, `"probe"`) and scored as ICC, not as unprobeable.

Tests: `test_min_n_guarded_icc_estimator_is_rejected[_cluster_agreement|_within_share]`, `test_no_module_callable_returns_one_way_icc`, `test_estimator_with_extra_required_parameter_is_rejected`, `test_estimator_with_neutral_extra_parameter_names_is_rejected`.

Coordinator mutants (same one-way ICC body, `if len(groups) < 5: raise ValueError`, appended to `pilot_draw.py`; gate + name-denylist only):

- name=`_cluster_agreement`: **1 failed, 1 passed in 0.33s** — `FAILED ...::test_no_module_callable_returns_one_way_icc`; name denylist passed.
- name=`_within_share`: **1 failed, 1 passed in 0.31s** — same gate fail; name denylist passed.
- name=`_rho_hat` (control): **2 failed in 0.31s** — name denylist *and* behavioural gate.

TEST-15 (throwaway copies of the test file):

- Mutant A, drop k≥5 probes: **2 failed in 0.42s** (`test_min_n_guarded_icc_estimator_is_rejected` both names).
- Mutant B, restore N15 abort-on-raise: **2 failed in 0.39s** (same two).

Clean inject tests (no append): **61 passed in 2.51s**.

## BR-52

What changed: unprobeable is now signature-less only. Missing annotations are dummy-filled; a helper is guilty only if a call returns the one-way ICC.

Tests: `test_ordinary_helper_is_not_a_reliability_estimator` for the four coordinator shapes plus `_join_labels`.

Legitimate shapes that still pass (inject + `_assert_no_reliability_estimator`):

- `annotated_kwonly_helper(*, left: str, right: str)`
- `unannotated_kwonly_helper(*, left, right)`
- `annotated_positional_helper(left: str, right: str)`
- `unannotated_positional_helper(left, right)`
- `_join_labels(left: str, right: str)` (N15's annotated extra-arg helper)
- existing `pilot_draw` helpers (`_require_int`, `_gold_count`, `_pick_one`, `_parse_stratum`, `_is_hard_entry`, `_load_selection_payload`, `_parse_entries`, …) via the clean suite

Proof:

- Four public coordinator helpers appended: ICC gate **1 passed in 0.43s** (`test_no_module_callable_returns_one_way_icc`).
- Four private (`_*`) copies appended: **60 passed in 3.73s** (pre +1 extra-arg test; ICC probe did not trip).
- TEST-15 mutant C, restore N15 "unknown param = unprobeable": **3 failed, 5 passed in 5.88s** — `unannotated_kwonly_helper` and `unannotated_positional_helper` RED; annotated kwonly/positional still GREEN. `_agreement_ratio2` also RED against the new "must be an ICC offender, not merely unprobeable" assertion.

BR-51 still RED after this narrowing: `_cluster_agreement` / `_within_share` gate failures above were taken on the narrowed probe.
