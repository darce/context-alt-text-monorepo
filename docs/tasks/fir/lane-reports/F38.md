# Lane F38 — FIR-12-BR-62

Gate after fix: `121 passed in 2.80s` (trio). Baseline was `117 passed`. Full scene suite: `1353 passed, 4 skipped, 1 deselected` (was 1349; +4 new tests). MappingProxyType pickle/asdict/deepcopy of `GallerySplit` fail on this Python; `copy.copy` and `dataclasses.replace` work. Nothing in the suite pickles a `RunPlan`/`GallerySplit`, so the proxy was kept.

- **BR-62** — `GallerySplit.g1` / `g2` are now `Mapping[str, Template]` stored as `MappingProxyType` after `_normalise_gallery_map`. In-place item assignment and `del` raise `TypeError`; `pop`/`update` raise `AttributeError` (proxy is not a `MutableMapping`). Pinned by `test_gallery_maps_reject_in_place_mutation` (assign / `del` / `pop` / `update` on both galleries) plus `test_replace_and_readonly_input_rerun_post_init` (`replace(split)` and constructing from the already-read-only maps re-run `__post_init__` and stay immutable). Clean (the four new tests): `4 passed in 0.05s`. Mutant under `/tmp/f38-br62-mutant`: `return MappingProxyType({subject: out[subject] for subject in sorted(out)})` → `return {subject: out[subject] for subject in sorted(out)}` → `FAILED ... DID NOT RAISE <class 'TypeError'>` / `3 failed, 1 passed in 0.14s` (`test_gallery_map_reads_survive_readonly_wrapper` stays green). Name-neutral mutant `/tmp/f38-br62-neutral` (`class Roster(dict): pass; return Roster(...)`): `FAILED ... DID NOT RAISE <class 'TypeError'>` / `1 failed in 0.09s`.

## Legitimate shapes still green

- Plain `dict` construction; already-read-only maps (`GallerySplit(g1=split.g1, ...)`); `dataclasses.replace(split)` and `replace(split, g1={" Alice ": ...})` re-strips.
- Internal whitespace `"Alice Smith"` (`test_internal_whitespace_subject_id_is_kept`); `{**split.g1}` reconstruction; `len` / `in` / `.get` / `.values()` / sorted iteration (`test_gallery_map_reads_survive_readonly_wrapper`).
- Existing BR-56 blank/None rejection, BR-59 collision, BR-60 sort, frozen-frame `g1=53 g2=56`.
- Adapter tests that mutated `plan.split.g1` (`test_blank_roster_subject_id_raises`, `test_roster_keeps_internal_whitespace_subject_id`) were rewritten: published handle now TypeErrors on assign; legal `"Alice Smith"` is reconstructed via `GallerySplit({**g1, name: extra})`; BR-58 blank `_roster` path is planted with `object.__setattr__` on a copied `dict`, not through the published map.
