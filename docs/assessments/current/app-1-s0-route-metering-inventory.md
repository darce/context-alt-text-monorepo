# APP-1 S0 route and metering inventory

| Path | Method | Auth dependency actually applied | Current metering or quota mechanism | Beta entitlement behavior required by Plan 0001 |
| --- | --- | --- | --- | --- |
| `/openapi.json` | GET, HEAD | none; FastAPI generated route | **UNAUTHENTICATED; UNMETERED** | `PUBLIC-BOOT`: keep public only if the beta host accepts schema disclosure; otherwise make the exposure decision explicit. |
| `/docs` | GET, HEAD | none; FastAPI generated route | **UNAUTHENTICATED; UNMETERED** | `PUBLIC-BOOT`: document whether interactive docs are disabled on the beta host. |
| `/docs/oauth2-redirect` | GET, HEAD | none; FastAPI generated route | **UNAUTHENTICATED; UNMETERED** | `PUBLIC-BOOT`: same host-level docs decision as `/docs`. |
| `/redoc` | GET, HEAD | none; FastAPI generated route | **UNAUTHENTICATED; UNMETERED** | `PUBLIC-BOOT`: same host-level docs decision as `/docs`. |
| `/recognition/analyze` | POST | Router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` and `enforce_demo_quota` | Per-key in-memory sliding-window rate limit plus atomic demo-key unit consumption; no beta tenant ledger | `BETA-METERED`: durable tenant reservation before costly work; one successful image job unit; terminal failure/cancel releases; retries do not double-charge. |
| `/recognition/jobs/{job_id}` | GET | Router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded polling is free, but remains authenticated and tenant-scoped. |
| `/recognition/jobs/{job_id}/acknowledge-projection` | POST | Router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: control acknowledgement is free; preserve tenant authorization. |
| `/recognition/jobs/{job_id}/stream` | GET | Router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: stream/poll transport is free and bounded. |
| `/recognition/jobs/{job_id}/cancel` | POST | Router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: cancellation is free and must release any outstanding reservation. |
| `/recognition/analyze/multipart` | POST | Handler `require_write_access`; handler `enforce_demo_quota`; no router-level rate dependency | Atomic demo-key unit consumption; no per-key rate limiter and no beta tenant ledger | `BETA-METERED`: converge with JSON analyze at the same durable admission boundary and byte budget. |
| `/recognition/blobs/{job_id}/{media_id}` | GET | Handler `require_auth` only | **UNMETERED**; no rate or customer quota dependency | `FREE-POLL`: bounded result retrieval is free, with tenant ownership enforced. |
| `/recognition/face-thumbs/{job_id}/{media_id}` | GET | Handler `require_auth` only | **UNMETERED**; no rate or customer quota dependency | `FREE-POLL`: bounded result retrieval is free, with tenant ownership enforced. |
| `/recognition/clustering/jobs` | POST | Router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` and `enforce_demo_quota` | Per-key rate limit plus atomic demo-key unit consumption; no beta operational ledger | `CLUSTER-BUDGET`: separate capped clustering budget, admitted before work and isolated from image-unit accounting. |
| `/recognition/clusters/recover-orphans` | POST | Router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` and `enforce_demo_quota` | Per-key rate limit plus atomic demo-key unit consumption; no beta operational ledger | `CLUSTER-BUDGET`: recovery is bounded operational work and must not become an image-unit bypass. |
| `/recognition/clusters` | GET | Snapshot router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded cluster listing is free; retain tenant isolation. |
| `/recognition/tenants/{tenant_uuid}/clusters/snapshot` | GET | Snapshot router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: free only after the route verifies the authenticated tenant can read the requested snapshot. |
| `/recognition/tenants/{tenant_uuid}/clusters/targeted-snapshot` | GET | Snapshot router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: same tenant-scoped read rule as snapshot. |
| `/recognition/tenants/{tenant_uuid}/clusters/delta` | GET | Snapshot router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded delta reads are free and tenant-scoped. |
| `/recognition/clusters/top-unlabeled` | GET | Snapshot router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: free cluster read; do not spend image allowance. |
| `/recognition/clusters/{cluster_id}/members` | GET | Snapshot router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: free cluster read; do not spend image allowance. |
| `/recognition/clusters/{cluster_id}/dismiss` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: define whether curation writes use the cluster operational budget or remain free control-plane work. |
| `/recognition/clusters/{cluster_id}/dismiss` | DELETE | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: define whether curation writes use the cluster operational budget or remain free control-plane work. |
| `/recognition/clusters/{cluster_id}` | PATCH | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify cluster metadata writes against the separate operational budget. |
| `/recognition/clusters/create-for-identity` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify identity-to-cluster creation against the separate operational budget. |
| `/recognition/clusters/{cluster_id}/merge` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify merge work against the separate operational budget and bound its cost. |
| `/recognition/clusters/{cluster_id}/split` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify split work against the separate operational budget and bound its cost. |
| `/recognition/topology-commands/split` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify topology commands against the separate operational budget. |
| `/recognition/clusters/reassign` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify reassignment against the separate operational budget. |
| `/recognition/clusters/revert-merge` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify merge reversal against the separate operational budget. |
| `/recognition/clusters/{cluster_id}/assign` | POST | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify assignment against the separate operational budget. |
| `/recognition/clusters/{cluster_id}/representatives/{representative_id}/pin` | PATCH | Topology router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify representative curation against the separate operational budget. |
| `/recognition/clusters/centroid-health` | GET | Maintenance router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: operational health read is free and bounded. |
| `/recognition/clusters/maintenance/refresh-centroids` | POST | Maintenance router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` and `enforce_demo_quota` | Per-key rate limit plus atomic demo-key unit consumption; no beta operational ledger | `CLUSTER-BUDGET`: admit and cap centroid work separately from image units. |
| `/recognition/clusters/events` | GET | Events router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded event polling is free. |
| `/recognition/suggestions` | GET | Suggestions router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify suggestion generation/read work and cap any costly path. |
| `/recognition/suggestions/merge` | GET | Suggestions router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify merge-suggestion work and cap any costly path. |
| `/recognition/clusters/{cluster_id}/roster-candidates` | GET | Suggestions router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify roster-candidate computation and cap any costly path. |
| `/recognition/identities/suggestions` | GET | Suggestions router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify identity suggestion computation and cap any costly path. |
| `/recognition/identities/{identity_id}/suggestions` | GET | Suggestions router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify identity suggestion computation and cap any costly path. |
| `/recognition/suggestions/name` | GET | Suggestions router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify name-suggestion computation and cap any costly path. |
| `/recognition/suggestions/{suggestion_id}/accept` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify suggestion mutation as free control-plane work or cluster budget. |
| `/recognition/suggestions/name/{suggestion_id}/accept` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify suggestion mutation as free control-plane work or cluster budget. |
| `/recognition/suggestions/name/{suggestion_id}/reject` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify suggestion mutation as free control-plane work or cluster budget. |
| `/recognition/suggestions/{suggestion_id}/reject` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify suggestion mutation as free control-plane work or cluster budget. |
| `/recognition/suggestions/bulk-accept` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: bound batch size and classify bulk mutation against the operational budget. |
| `/recognition/suggestions/merge/{suggestion_id}/accept` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify merge-suggestion mutation as free control-plane work or cluster budget. |
| `/recognition/suggestions/merge/{suggestion_id}/reject` | POST | Suggestions router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify merge-suggestion mutation as free control-plane work or cluster budget. |
| `/recognition/diagnostics/decisions` | GET | Diagnostics router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded diagnostics read is free. |
| `/recognition/media/identities` | GET | Media router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded metadata read is free. |
| `/recognition/retention/policy` | GET | Retention router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: policy read is free. |
| `/recognition/retention/policy` | PATCH | Retention router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify retention policy writes and bound their operational cost. |
| `/recognition/retention/policy/preset` | POST | Retention router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify retention preset writes and bound their operational cost. |
| `/recognition/retention/export` | POST | Retention router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify export admission, byte budget, and any separate operational allowance. |
| `/recognition/retention/export/{job_id}/status` | GET | Retention router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: export status polling is free and bounded. |
| `/recognition/retention/export/{job_id}/data` | GET | Retention router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: authorized export retrieval is free; bound response bytes. |
| `/recognition/retention/purge` | POST | Retention router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify purge work and bound its operational cost. |
| `/recognition/retention/audit` | GET | Retention router `require_auth` plus `enforce_rate_limit` | **UNMETERED** customer usage; per-key rate limit only | `FREE-POLL`: bounded audit read is free. |
| `/recognition/retention/import` | POST | Retention router `require_auth` plus `enforce_rate_limit`; handler `require_write_access` | **UNMETERED** customer usage; per-key rate limit only | `OPEN-Q`: classify import byte budget and admission boundary. |
| `/recognition/tenant/whoami` | GET | Tenant router `require_auth_key_only` plus `_enforce_rate_limit_key_only` which calls `enforce_rate_limit` | **UNMETERED** customer usage; key-only rate limit only | `FREE-POLL`: Test Connection/identity discovery is free and must not consume image units. |
| `/recognition/tenant/naming-agreement` | GET | Router `require_auth_key_only` plus key-only rate limit; handler also `require_auth` | **UNMETERED** customer usage; key-only rate limit only | `FREE-POLL`: naming-agreement read is free. |
| `/recognition/tenant/naming-agreement` | PUT | Router `require_auth_key_only` plus key-only rate limit; handler `require_write_access` | **UNMETERED** customer usage; key-only rate limit only | `OPEN-Q`: classify this control-plane mutation; it must not bypass tenant admission if later made costly. |
| `/roster/curation/sync` | POST | `get_tenant_id` only; it validates a header/query tenant id but applies no API auth dependency | **UNAUTHENTICATED; UNMETERED**; no rate or quota dependency | `BETA-METERED` or an explicit exclusion: S0 must close the unauthenticated route before beta and decide whether sync consumes an operational budget. |
| `/scene/describe/multipart` | POST | Handler `require_write_access`; no router-level auth or rate dependency | Demo-key `maybe_consume_demo_quota` in compute path; no beta tenant ledger or per-key rate limiter | `BETA-METERED`: reserve before image work, enforce request byte cap, and release on terminal failure/cancel. |
| `/scene/describe/async` | POST | Handler `require_write_access`; no router-level auth or rate dependency | Demo-key `maybe_consume_demo_quota` before process-local `AsyncAdmissionGate.try_acquire`; no durable beta ledger | `BETA-METERED`: durable reservation and bounded byte/job admission must precede dispatch; failed admission must not leak or double-charge. |
| `/scene/describe/jobs/{job_id}` | GET | Handler `require_write_access` | **UNMETERED** customer usage; no rate or quota dependency | `FREE-POLL`: authenticated job polling is free and bounded. |
| `/scene/describe/run` | POST | Handler `require_write_access` | `maybe_consume_demo_quota` with units equal to unique media IDs; durable run idempotency but no beta tenant ledger | `BETA-METERED`: reserve by operation id before dispatch, charge successful images once, and release failed/cancelled work. |
| `/scene/describe/run/{run_id}` | GET | Handler `require_write_access` | **UNMETERED** customer usage; no rate or quota dependency | `FREE-POLL`: run status is free and bounded. |
| `/scene/describe/run/{run_id}/items` | GET | Handler `require_write_access` | **UNMETERED** customer usage; no rate or quota dependency | `FREE-POLL`: item status is free and bounded. |
| `/scene/describe/run/{run_id}` | DELETE | Handler `require_write_access` | **UNMETERED** customer usage; no rate or quota dependency | `FREE-POLL`: cancel/delete control is free and must release any reservation. |
| `/scene/gpu/status` | GET | Handler `require_auth` | **UNMETERED**; no rate or customer quota dependency | `FREE-POLL`: operational status is free; do not spend image allowance. |
| `/scene/gpu/intent` | POST | Handler `require_write_access` | **UNMETERED**; no rate or customer quota dependency | `CLUSTER-BUDGET`: if customer-reachable in beta, gate it under a separately capped operational budget or remove it from the public surface. |
| `/x/provision` | POST | Demo router `enforce_ip_rate_limit` plus handler `enforce_provision_rate_limit`; no API-key auth | Per-IP and per-source provisioning sliding windows; no customer beta quota | `PUBLIC-BOOT`: keep as a separately controlled demo onboarding path, never as a beta entitlement bypass. |
| `/x/{slug}/session` | POST | Demo router `enforce_ip_rate_limit`; session issuance handler; no API-key auth | Per-IP sliding-window rate limit; no customer beta quota | `PUBLIC-BOOT`: retain only for the demo surface and isolate its credentials from beta tenant accounting. |
| `/x/{slug}` | GET | Demo router `enforce_ip_rate_limit`; handler validates demo session token/header/cookie; no API-key auth | Per-IP sliding-window rate limit plus demo session check; no customer beta quota | `PUBLIC-BOOT`: demo traffic remains separately capped and cannot select a beta tenant. |
| `/admin/tenants` | POST | Conditional admin router dependency `require_admin`; handler `require_admin_header` | **UNMETERED** customer usage; admin auth only | `OPEN-Q`: operator provisioning is outside customer entitlement; keep it isolated and auditable. |
| `/admin/tenants` | GET | Conditional admin router dependency `require_admin` | **UNMETERED** customer usage; admin auth only | `OPEN-Q`: operator listing is outside customer entitlement; preserve Tailscale/admin boundary. |
| `/admin/tenants/{tenant_id}/keys` | POST | Conditional admin router `require_admin`; handler `require_admin_header` | **UNMETERED** customer usage; admin auth only | `OPEN-Q`: operator mint remains outside beta usage; reuse the local key authority. |
| `/admin/tenants/{tenant_id}/keys` | GET | Conditional admin router dependency `require_admin` | **UNMETERED** customer usage; admin auth only | `OPEN-Q`: operator key history remains outside beta usage; preserve audit and tenant scope. |
| `/admin/keys/{key_id}/revoke` | POST | Conditional admin router `require_admin`; handler `require_admin_header` | **UNMETERED** customer usage; admin auth only | `OPEN-Q`: emergency operator revoke remains free and must remain immediately effective. |
| `/admin/` | GET | Conditional admin router dependency `require_admin`; handler `get_admin_session` | **UNMETERED** customer usage; admin auth only | `OPEN-Q`: admin console is not a beta entitlement surface. |
| `/admin/ui/tenants` | POST | Conditional admin router `require_admin`; handler `require_same_origin` | **UNMETERED** customer usage; admin auth only; same-origin check | `OPEN-Q`: preserve the existing browser mutation boundary; do not reuse it as portal auth. |
| `/admin/ui/keys` | POST | Conditional admin router `require_admin`; handler `require_same_origin` | **UNMETERED** customer usage; admin auth only; same-origin check | `OPEN-Q`: preserve operator-only minting and one-time secret display. |
| `/admin/ui/keys/{key_id}/revoke` | POST | Conditional admin router `require_admin`; handler `require_same_origin` | **UNMETERED** customer usage; admin auth only; same-origin check | `OPEN-Q`: preserve operator-only atomic revoke and audit behavior. |
| `/health` | GET | `get_observability_session`; no customer auth | **UNAUTHENTICATED; UNMETERED** | `FREE-POLL`: health is free and public only to the extent accepted by deployment policy. |
| `/ready` | GET | `get_observability_session`; no customer auth | **UNAUTHENTICATED; UNMETERED** | `FREE-POLL`: readiness is free and public only to the extent accepted by deployment policy. |
| `/health/detailed` | GET | `require_auth` plus `get_observability_session` | **UNMETERED**; auth only, no customer quota | `FREE-POLL`: observability is free and must not consume beta units. |
| `/metrics` | GET | `require_auth` | **UNMETERED**; auth only, no customer quota | `FREE-POLL`: metrics are free and operational; keep them out of customer usage accounting. |
| `/version` | GET | none; `register_version_route` intentionally exposes it before auth | **UNAUTHENTICATED; UNMETERED** | `PUBLIC-BOOT`: retain only as an intentional deployment/version probe; it is not customer work. |

| Security-relevant set | Routes explicitly flagged by the inventory |
| --- | --- |
| Unauthenticated or lacking service/API authentication | `/openapi.json`, `/docs`, `/docs/oauth2-redirect`, `/redoc`, `/roster/curation/sync`, `/health`, `/ready`, `/version`; the `/x/*` demo routes use IP/session controls rather than API-key authentication. |
| Unmetered or not customer-quota-gated | Every row marked **UNMETERED**, including all polling, curation, retention, admin, health, metrics, version, and GPU control routes; `/roster/curation/sync` is both unauthenticated and unmetered. The current demo-key quota is not a beta tenant entitlement ledger. |
