# Lane F6 — DESCQUAL-2 judgment-pool pins

- **DESCQUAL-2-BR-07.** Polarity is now load-bearing on the `_judged_keys` read-back path. `test_candidate_fact_judgment_does_not_mark_complementary_polarity` builds a dual-polarity "red hat" pool, judges only `CandidateFact(..., TRUE)`, and asserts `unjudged_count == 1` with the FALSE slot still open (union with the TRUE `PooledFact` is a no-op; union with FALSE closes the pool). Mutant `/tmp/dq2-f6-drop-polarity` (`pool_key(raw.text)`) → `judged_count` 0==1 RED; old six stay green. Mutant `/tmp/dq2-f6-invert-polarity` (TRUE↔FALSE) → `same_slot.judged_count` 2==1 RED.
- **DESCQUAL-2-BR-07 string branch (intended, not a defect).** `matched.update(by_text.get(...))` is a text-level judgment: a bare `str` has no polarity, `by_text` has been `dict[str, list[str]]` since the original commit (the list exists only because complementary keys share text), and polarity-specific matching is the typed `CandidateFact` / exact-key path. Encoded in `test_bare_string_judgment_marks_every_polarity_sharing_that_text` (`judged=["red hat"]` → `judged_count == 2`; exact pool-key string stays polarity-specific). Mutant `/tmp/dq2-f6-string-unique` (match only when `len(keys)==1`) → `judged_count` 0==2 RED.
- **DESCQUAL-2-BR-08.** `test_depth_truncates_per_contributor_not_globally` now parametrizes `depth=2` and `depth=3` over 4-item contributors. Mutant `/tmp/dq2-f6-depth-2` (`[:2]` instead of `[:depth]`) → depth=3 case RED (`['a1','a2','b1','b2'] != ['a1','a2','a3','b1','b2','b3']`); depth=2 stays green.
