# Lane F39 — FIR-12-BR-64

Baseline (starting tree `bbfb8239`): narrow `121 passed in 3.45s`; full `5 failed, 1648 passed, 4 skipped, 1 deselected` (the 5 are `test_real_cli_*` missing clone `.venv`). After fix: narrow `125 passed in 2.80s`; full `5 failed, 1652 passed, 4 skipped, 1 deselected` (+4 new tests; same 5 env fails).

- **BR-64** — `mated_identities_for` now strips probe identities with a local `_normalise_subject_id` (twin of `gallery_split._normalise_subject_id`) before the roster membership test, so a padded probe is still a mate of its stripped gallery key. Did not strip in `_identities` (that would hide a membership revert). Pinned by `test_both_padded_identity_stays_mated_and_fnir_measurable` (same `"Bob "` on E_clean and B_eyewear → G1 mated=`['Bob']`, media 11 not a foil, B_eyewear FNIR `0.000` not `"not measured"`), `test_direct_padded_probe_entry_matches_stripped_roster` (ProbeEntry bypass of manifest parse), and `test_probe_identity_normaliser_matches_gallery_split` (byte-for-byte vs `gallery_split._normalise_subject_id` on plain / lead / trail / both / tab / internal space / empty / non-string). Clean (the four new tests): `4 passed in 0.45s`. Mutant `/tmp/f39-br64-tree`: membership reverted to `if subject_id in roster` (helper still strips) → `FAILED assert [] == ['Bob']` / `2 failed, 2 passed in 0.43s` (agreement + Van Dyke stay green). Name-neutral mutant `/tmp/f39-br64-neutral`: helper still named `_normalise_subject_id` but `key = value` (no strip) → `FAILED assert [] == ['Bob']` plus agreement `' Bob' == 'Bob'` / `3 failed, 1 passed in 0.44s`.

## Legitimate shapes still green

- Unpadded Alice/Bob census (`test_padded_g1_roster_key_keeps_mated_partition` / `_g2_`, `test_group_still_yields_one_mated_unit_per_enrolled_subject`).
- BR-56 E_clean-only padding (probe half unpadded) still mates.
- Internal space is not collapsed: `"Van Dyke"` ≠ `"VanDyke"` (`test_internal_whitespace_subject_is_not_collapsed_into_a_mate`); stays green on both mutants (`1 passed`).
- Agreement table keeps `"Van Dyke"` / `"VanDyke"` as distinct return values; empty / `"   "` / `123` / `None` / `True` still raise on both normalisers.
