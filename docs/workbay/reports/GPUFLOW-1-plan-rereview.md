FINDINGS: [{"id":"GPUFLOW-1-RR-01","severity":"medium","file_path":"docs/tasks/v0.5.0/GPUFLOW-1-description-service-flow-and-identity-fixes-task-plan.md","line":488,"summary":"GPUFLOW-1-PR-07 remains partially resolved: fixture-dependent lane edges are ordered but their transferred artifacts are not named.","evidence":"The Lane Decomposition labels dependencies as artifacts, but naming-control -> suggestion-cards -> picker-undo, backend fixture producers -> PHP, PHP -> SPA, and UI -> UX rows use generic fixture descriptions; the plan defers exact fixture paths and ownership to materialization."},{"id":"GPUFLOW-1-RR-02","severity":"low","file_path":"docs/tasks/v0.5.0/GPUFLOW-1-description-service-flow-and-identity-fixes-task-plan.md","line":214,"summary":"Verification commands contradict the lane interpreter rule by prescribing bare python3 while the Lane Decomposition requires the provisioned lane-root interpreter and package-origin verification.","evidence":"Verification Strategy lines 213-215 uses python3 -m pytest, while Lane Decomposition line 450 requires the lane-root interpreter and import-origin verification; no exact frozen test command replaces the broad commands before dispatch."}]
Verdict: pass_with_findings

# GPUFLOW-1 plan re-review

This review uses the authoritative handoff IDs. The plan states at line 144 that the inlined astra review uses different numbering, so the table below follows the eight IDs in the handoff DB.

| id | status | plan section | evidence |
| --- | --- | --- | --- |
| GPUFLOW-1-PR-01 | resolved | Slice A1: Typed cold-GPU response and demand lease (lines 255–270) | Lines 261–264 register demand only at the actual GPU compute boundary, persist a tenant-scoped lease before publication/response, retain it after 503, renew the same operation, aggregate every publisher, reject stale snapshots, and preserve STOP/max-lease authority. Lines 269–270 cover pre-poll completion, concurrent publication, renewal, expiry, and cross-process stale writes. |
| GPUFLOW-1-PR-02 | resolved | Slice A1 eligibility and Slice A2 breaker (lines 261–285) | Lines 261–262 exempt CPU/hosted/default/cache paths and distinguish Auto startup from STOP/stopping/unavailable/unknown. Lines 278–285 put counters, locks, open state, and resets on the canonical base-URL/route-family key and validate typed warming before failure accounting. |
| GPUFLOW-1-PR-03 | resolved | Terminology; Slice B1/B2 (lines 65–69, 303–334) | Lines 65–68 retain ramp-up/startup correlation and rename service timing to server elapsed, excluding client render. Lines 309–311 persist operation/startup IDs and first readiness across retries, callers, expiry, restart, warm, and cache paths; line 328 renders only wire values. |
| GPUFLOW-1-PR-04 | resolved | Slice B1 and timing lane decomposition (lines 309–319, 455–462) | Lines 309–314 name both shared schemas, strict response models, migration/model storage, repositories, worker/router builders, and histogram ownership. Lines 455–462 split contracts, models, repositories, response models, instrumentation, sync route, and run route into bounded producer lanes; lines 318–319 require real-builder round trips including failed/cancelled/untimed values. |
| GPUFLOW-1-PR-05 | resolved | Slice A1: eligibility and lifecycle states (lines 261–270) | Line 261 gates demand/readiness after validation, adapter selection, and cache/decorative bypasses; CPU, hosted, and non-GPU defaults remain usable. Line 262 gives separate STOP, stopping, unavailable, and stale/unknown behavior, with tests listed at lines 269–270. |
| GPUFLOW-1-PR-06 | resolved | Slice C4: Quality-aware representative selection (lines 380–394) | Lines 386–389 move calibrated representative-only scoring into the selector/writer lanes, compare the eligible existing/new union, preserve pins/diversity, and leave assignment thresholds unchanged. Lines 393–394 require clearer unpinned pose-bucket and pose-free newcomers to replace old representatives while pins remain unchanged. |
| GPUFLOW-1-PR-07 | partially resolved | Lane Decomposition, Collision map, and Merge Order (lines 452–502) | Lines 488 and 497–499 now serialize naming-control → suggestion-cards → picker-undo and describe producer-owned fixtures, but the dependency cells still say only “fixtures”/“integrated ... fixtures” without a named path or committed artifact. See GPUFLOW-1-RR-01. |
| GPUFLOW-1-PR-08 | resolved | Open decisions and Slice D2 (lines 140–142, 410–420) | The operator/API-R-23 owner is named; the default is preserve existing behavior and block dispatch until a brief is accepted. Slice D2 requires numeric cap, bounded field, admission-vs-undo scope, typed status/code, and zero/cap/cap+1/malformed/separate merge-undo tests. |
| GPUFLO-L-01 | resolved | Open decisions and orchestration gate (lines 144, 495–510) | The plan records commit 59ced8b as report-only review evidence, says its static checks have no runtime behavior, and requires luna re-review/freeze before materialization. The gate also preserves evidence and forbids this lane from editing the manifest or sibling reports. |

## FINDINGS

### GPUFLOW-1-RR-01 — medium

- **Section:** Lane Decomposition — Lanes, Collision map, and Manifest (lines 452–510).
- **Evidence:** The dependency column is explicitly labeled “artifact transferred,” but the naming-control → suggestion-cards → picker-undo edges at lines 470, 474, and 488 identify only generic naming/group-preview/candidate/null “fixtures.” The same omission appears for serialized multipart/run fixtures into `php-breaker-passthrough` (lines 461–463), PHP wire fixtures into `spa-describe-client` (line 465), and UI fixtures into the UX lanes (lines 476–479). Line 510 defers expansion and fixture pinning until after freeze.
- **Fix:** Before materialization, give every fixture edge an exact committed path or artifact ID, name its producer and read-only consumers in the lane manifest, and make `lane_dag` validate those artifact dependencies. In particular, pin the three shared identity fixture handoffs so the existing sequence cannot be lost when rows are materialized.

### GPUFLOW-1-RR-02 — low

- **Section:** Verification Strategy versus Lane Decomposition (lines 213–219 and 450–510).
- **Evidence:** The global service test commands use bare `python3 -m pytest` and broad `-k` expressions at lines 214–215, while line 450 requires the provisioned lane-root interpreter and package-origin verification. The visible manifest contains lane IDs only, and line 510 still leaves exact test-file expansion for a later materialization step.
- **Fix:** Replace every Python test placeholder in the frozen dispatch manifest with an exact lane-root interpreter command and test path, including the import-origin check; retain broad `-k` commands only as optional aggregate verification after lane-local tests.
