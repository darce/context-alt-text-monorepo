{
  "servers": {
    "context-alt-text": {
      "command": "${workspaceFolder}/scripts/mcp/mcp-server.sh",
      "args": [
        "run"
      ],
      "env": {
        "PATH": "${env:PYENV_ROOT}/bin:${env:PYENV_ROOT}/shims:/opt/homebrew/bin:/usr/local/bin:${env:PATH}",
        "PYENV_ROOT": "${env:PYENV_ROOT}"
      }
    }
  }
}
