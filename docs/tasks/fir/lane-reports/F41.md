# Lane F41 — FIR-12-BR-63

Convention: JANUS 2.3.4 FPI (`score > tau`) for `fmr_at`. Reason: the tau this module chooses is the operating point `fnir_fpi_at_threshold` publishes; `fnmr_at` (`score < tau`) already matched FNIR. Scorer left untouched.

Narrow baseline: `36 passed in 0.56s`. After: `37 passed in 0.45s`. Full gate: `5 failed, 1649 passed, 4 skipped, 1 deselected` (was 1648 passed; +1 test). The 5 are the pre-existing `test_real_cli_*` missing-venv cases.

- **BR-63** — `fmr_at` now counts `s > tau` (JANUS FPI); OOF `n_imp_accept` uses the same predicate. `fnmr_at` unchanged. Cross-module pin: `test_calibrated_tau_agrees_with_published_fpi_and_fnir_on_tie` (impostors `[0.6, 0.55, 0.4]`, genuines `[0.9, 0.55, 0.4]`, `fmr_target=1/3` so chosen tau lands on an observed impostor; calibration accept-count == published FPI; `fnmr_at` == FNIR). Clean: `1 passed in 0.05s`. Mutant `/tmp/f41-mut-fmr` (`s > tau` → `s >= tau`): `FAILED ... assert 1.0 == 0 ± 1.0e-12` / `1 failed in 0.12s`. Opposite mutant `/tmp/f41-mut-fnmr` (`fnmr_at` `s < tau` → `s <= tau`): `FAILED ... assert 0.666... == 0.333...` / `1 failed in 0.13s`. Mated-side agreement already held before the FPI fix; the FNMR mutant shows the test pins agreement, not a hard-coded rate.

Existing `fmr_at` tie tests rewritten to the JANUS expectation, not the new output:

- `test_fmr_at_and_fnmr_at_direct`: `fmr_at([0.9, 0.4, 0.1], 0.4)` is `1/3` (only 0.9 is strictly above), not `2/3`. Added `fmr_at([0.4], 0.4) == 0` and `fnmr_at([0.4], 0.4) == 0` (tie is not FPI; tie is a mate hit).
- `test_select_threshold_unit_behavior`: impostors all `0.1` at target 0 now select `tau=0.1` (none strictly `> 0.1`); tight target selects `max(impostors)` with FMR=0; impostor `1.0` at target 0 selects `tau=1.0` (tie is not FPI), not fail-closed above 1.0.
- `test_golden_numeric_pins_on_committed_fixture`: inclusive FMR used to walk past the tie, pinning global tau `0.66` / occlusion `0.45` / OOF FMR `0` / FNMR `2/3` / people OACT tax `11/21`. JANUS FPI lets the observed-impostor candidate stand: tau `0.58`, occlusion `0.38`, OOF FMR `1/13`, FNMR `15/24`, people OACT tax `7/21`. Same fixture, honest operating point.

## Legitimate shapes still green

- Empty scores → `None`; all `-inf` impostors → FMR 0; genuine `== tau` is a hit; impostor `> tau` is still accepted.
- `select_threshold` still abstains on zero / all-nonfinite impostors; empty genuines still select from impostors; `fmr_target=0.01` walk still meets the target.
- Scorer split unchanged: `test_fpi_uses_strict_gt_fnir_uses_at_or_above` (FPI 0 on a tie, FNIR 0 on a mate at tau).
- Golden fixture still produces `tau > 0`, no fail-open; pair-level disjointness, schema, stranger-once, and CLI determinism tests still pass.
